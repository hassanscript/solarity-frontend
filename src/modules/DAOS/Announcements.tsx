import React, { FC } from "react";
import Base from "modules/DAOS/Base";

const Announcements: FC<{ dao: any }> = ({ dao }) => {
  return (
    <Base>
      <>Hello wolrd</>
    </Base>
  );
};

export default Announcements;
